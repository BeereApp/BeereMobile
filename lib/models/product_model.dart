import 'package:flutter/material.dart';

class ProductModel {
  int id;
  String name;
  String title;
  String price;
  String rating;
  String imageUrl;
  String description;
  ProductModel(this.id, this.name, this.title, this.price, this.rating, this.imageUrl,
      this.description);
}
